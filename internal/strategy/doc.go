// Package strategy holds trading-decision logic. The concrete strategy is
// undecided; model it behind an interface so the trading loop depends on the
// abstraction, not a specific algorithm.
package strategy

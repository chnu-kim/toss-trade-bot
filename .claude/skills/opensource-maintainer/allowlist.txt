# opensource-maintainer 시크릿 스캔 allowlist (enforcement 설정 — CODEOWNERS 보호 대상).
#
# 형식: <레포 루트 상대 경로>|<스캐너가 뽑아낸 '매치된 텍스트' 전체>|<사유>
#
# 매칭 규칙(의도적으로 좁다): **경로가 정확히 일치**하고 **매치된 텍스트가 리터럴과
# 완전히 같을** 때만 그 매치 하나가 제외된다. 따라서
#   - 같은 파일의 *다른* 값은 계속 검출된다(파일 전체를 여는 게 아니다).
#   - 같은 값이 *다른* 파일에 나타나면 계속 검출된다(값만으로 열리지 않는다).
#   - 허용값을 접두/접미로 품은 *더 긴* 값(`<허용값>PLUS_REAL_SECRET`)은 다른 텍스트이므로
#     계속 검출된다(부분일치 우회 차단).
#   - 한 줄에 매치가 여러 개면 나머지 매치는 그대로 평가된다.
#   - 디렉토리/글롭/패턴 단위 제외는 지원하지 않는다.
#
# 리터럴은 '값'이 아니라 스캐너가 뽑아낸 매치 토큰 전체를 그대로 적는다(공백·따옴표 포함).
# 확인 방법: 스캔을 돌려 후보 줄을 찾은 뒤, 그 줄에서 실제 매치 토큰을 확인해 붙여넣는다.
#
# 왜 소스 인라인 주석(`# nolint` 류)이 아니라 이 매니페스트인가:
# scan.sh는 CI에서 PR 내용을 검사하는 자동 게이트다. 인라인 마커를 전역으로 인정하면
# PR이 진짜 자격증명 옆에 마커만 달아 게이트를 통과할 수 있다(스캔 대상과 우회 수단이
# 같은 신뢰 경계 안에 있게 된다). 이 파일은 CODEOWNERS로 보호되므로 항목 추가에
# 소유자 리뷰가 필요하다 — 우회가 enforcement 설정 변경으로 승격된다.
#
# 항목을 추가하기 전에: 그 값이 정말 합성(가짜)인지 확인하라. 실제 시크릿은 절대
# allowlist 하지 말고 제거·회전한다.

internal/config/config_test.go|Secret = "raw-test-secret-do-not-log|L-9(로그 유출 금지) 검증용 합성 상수 — 실제 시크릿 아님

# 바로 위 항목의 리터럴이 이 파일에도 그대로 적혀 있어 스캐너가 이 매니페스트 자신을
# 후보로 잡는다. 이 파일은 통째로 제외하지 않고 스캔하므로(실제 자격증명이 매니페스트에
# 들어가는 것도 잡아야 한다) 그 자기참조 줄만 완전일치로 허용한다.
.claude/skills/opensource-maintainer/allowlist.txt|Secret = "raw-test-secret-do-not-log|위 항목의 자기참조 줄

# Compiled binaries
/bin/
/toss-trade-bot
*.exe

# Secrets / local env (never commit credentials)
.env
.env.*
!configs/env.example

# Dependencies
/vendor/

# SQLite store artifacts (live order/halt state — never commit)
*.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
*.swp
